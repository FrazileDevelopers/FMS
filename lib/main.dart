import 'package:flutter/material.dart';
import 'package:fms/pages/firstUI.dart';

void main() => runApp(MyApp());

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: FirstUI(),
    );
  }
}
// void main() {
//   runApp(FirstUI());

// }
